%h1.large-heading.text-center Write a Review
%h4.text-center for listed Companies below

- @companies.each do |company|
  .review
    .row
      .col-md-5
        %span.cl-effect-12
          %a(href='#' class='company-name')
            = company.name
        .col-md-12.company-stars
          - company.categories.each do |category|
            = link_to category.title, '#', class: 'label label-primary'
        .review-stars.col-md-12.company-stars.average-stars
          = fa_icon 'star'
          = fa_icon 'star'
          = fa_icon 'star'
          = fa_icon 'star-o'
          = fa_icon 'star-o'
          %span.sum-reviews.company-reviews 116 Reviews
          .col-md-12.eur.eur-all-search
            - full_euro_icons = company.price_range
            - 5.times do
              - if full_euro_icons > 0
                = fa_icon 'eur', class: 'eur-full'
                - full_euro_icons -= 1
              - else
                = fa_icon 'eur', class: 'eur-empty'
      .col-md-5.address
        %div
          = "#{company.city.name}, #{company.city.country}"
      .col-md-2
        %br
        %br
        = link_to 'Write  a Review', new_user_company_review_path(company.owner, company), class: 'btn btn-primary'

    %hr

